import React from 'react';
import SignIn from './SignIn';

/**
 * Public Customer Registration is unified with the Mobile Number + Real SMS OTP flow.
 * No email or password creation required.
 */
const SignUp = () => {
  return <SignIn />;
};

export default SignUp;
